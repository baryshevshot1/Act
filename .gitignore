# ====================================================================
# Act — .gitignore
# Python (Django) + Node (Next.js) + macOS + dev tooling
# ====================================================================

# === Environment ===
.env
.env.local
.env.*.local
!.env.example

# === Python / Django ===
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
*.egg-info/
*.egg
dist/
build/
*.manifest
*.spec

# Django
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal
media/
staticfiles/
backend/static/

# Tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
.tox/
.nox/
.coverage
.coverage.*
htmlcov/
.cache/

# Migrations (oprionally tracked; keep tracked for now)
# backend/apps/*/migrations/ — ИЗ-ПОД RUN: миграции ОТСЛЕЖИВАЮТСЯ в git
# (см. CLAUDE.md "Migrations: атомарные" + Iteration 4 skill write-rls-policy)

# Procrastinate runtime
.procrastinate/

# === Node / Next.js ===
node_modules/
.pnpm-store/
.pnpm-debug.log*
.npm/
.yarn/
.next/
.turbo/
out/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Next.js build artifacts
.swc/
next-env.d.ts

# === IDEs / editors ===
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
.idea/
*.swp
*.swo
*~

# === OS ===
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# === Docker volumes (если используется local Docker volumes outside of named volumes) ===
postgres_data/

# === Secrets / keys / certs (защита от случайного коммита) ===
*.pem
*.key
*.crt
*.p12
secrets/
.secrets/

# === Coverage / test artifacts ===
.coverage
coverage/
*.cover
*.py,cover
.hypothesis/

# === Sentry / observability local artifacts ===
.sentryclirc

# === Misc ===
*.bak
*.tmp
*.orig
.history/

# === Claude Code local conversation cache (если есть) ===
.claude/cache/
.claude/sessions/

# === Local-only experiments ===
playground/
sandbox/
